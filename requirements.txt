# Core — no external dependencies for the pipeline itself
# (Python stdlib only: json, xml, csv, argparse, pathlib, logging, uuid)

# ── Optional: annotation visualisation ───────────────────────────────────────
# Install to use draw_annotations.py:
Pillow>=10.0

# ── Development & testing ─────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
